# Body Reset Treatment — τι εκκρεμεί

Φάση 1 (Αρχική + Ποιοι Είμαστε) είναι χτισμένη. Τα παρακάτω είναι **placeholders
που ΠΡΕΠΕΙ να αντικατασταθούν πριν βγει οτιδήποτε live.**

## 🔴 Μπλοκάρουν το launch

| Τι | Πού | Σημείωση |
|---|---|---|
| **Τηλέφωνο** | `src/content/site.ts` → `phone` | Τώρα `210 000 0000` |
| **Email** | `src/content/site.ts` → `email` | Τώρα `info@bodyresettreatment.gr` |
| **Διεύθυνση + ΤΚ + πόλη** | `src/content/site.ts` → `address` | Τώρα υποθετική **Αθήνα** — αν είναι άλλη πόλη αλλάζουν και τα SEO keywords |

| **Domain** | `astro.config.mjs` + `site.ts` → `url` | Τώρα `bodyresettreatment.gr` (υπόθεση). Πρέπει να ταιριάζουν τα δύο. |
| **Κείμενα 3 θεραπειών** | `src/content/services.ts` (`@draft`) | Deep tissue · Θεραπευτικό μασάζ · Theragun therapy — ήρθαν ως ονόματα μόνο. Ό,τι γράφτηκε είναι δικό μας, χωρίς ισχυρισμό αποτελέσματος |
| **Φωτό Theragun** | `public/images/services/theragun-therapy-masaz-athina.webp` | **Δεν δείχνει τη συσκευή** — καμία λήψη δεν την έχει. Η μόνη ασυμφωνία εικόνας/περιεχομένου στο site |
| **OG εικόνα** | `public/images/general/og.jpg` | Ακόμη placeholder — αυτή βλέπει κάποιος όταν μοιράζεται τον σύνδεσμο σε Facebook/WhatsApp/Viber |

## ✅ Έτοιμο

**Φωτογραφία hero — τελική, από τον πελάτη.** Δύο λήψεις: οριζόντια για υπολογιστή,
κάθετη για κινητό, με art direction κατά **προσανατολισμό** (όχι πλάτος). Είναι
**ανοιχτόχρωμες** — το hero γύρισε ολόκληρο σε σκούρα μελάνη (`ink="dark"`):
λογότυπο, δείκτης scroll, μπάρα μενού και η κατεύθυνση του scrim. Το λογότυπο
ανέβηκε στο άδειο πάνω μέρος του κάδρου· χαμηλά έπεφτε πάνω στο σώμα.


**Λογότυπο — vector, αυτούσιο** (`raw/BODY RESET_logo_final.svg`, Illustrator export).
Δεν ξανασχεδιάστηκε τίποτα: τα path data είναι byte-for-byte του αρχείου. Το build
script (`make-logo-svg.py`) κάνει **μόνο δύο** πράγματα — αφαιρεί το μαύρο `<rect>`
φόντο (ώστε να κάθεται πάνω στη φωτογραφία χωρίς μαύρο κουτί) και αλλάζει το
`viewBox`, που είναι καθαρό crop.

Μετρήσεις με `getBBox` (πηγή viewBox `0 0 425.2 425.2`):

| Στοιχείο | x | y |
|---|---|---|
| κύμα | 80.00 → 345.83 | 224.16 → 240.36 |
| κάθετος | 80.60 → 201.30 | 131.60 → 310.90 |
| BODY RESET | 126.20 → 297.10 | 259.90 → 275.30 |
| TREATMENT | 172.80 → 248.80 | 288.70 → 294.20 |

- `logo-full.svg` — viewBox `79 130.6 267.9 181.3`. **Μία έκδοση παντού**: hero και
  μπάρα δείχνουν το ίδιο, ολόκληρο artwork (κύμα + κάθετος + wordmark), κομμένο
  μόνο στο άδειο περιθώριο.
  Στην μπάρα: `clamp(4.5rem, 8.5vw, 7.5rem)` → **72px κινητό, 120px desktop**,
  μεγαλύτερο από την κλίμακα της αναφοράς (56/89px) γιατί το wordmark κάθεται
  χαμηλά σε ψηλή σύνθεση.

  **Γιατί το «TREATMENT» παραμένει μικρό:** η κάθετος μόνη της πιάνει 179 από τις
  181 μονάδες ύψους του artwork, οπότε το TREATMENT είναι μόλις **5.5 μονάδες = 3%**
  του συνολικού ύψους. Στα 120px βγαίνει ~3.6px. Για να φτάσει τα ~7px θα χρειαζόταν
  λογότυπο **~230px** — banner, όχι μπάρα. Είναι περιορισμός των αναλογιών του
  λογότυπου, όχι ρύθμιση. Η καθαρή λύση αν ενοχλεί: **οριζόντιο lockup** από τον
  σχεδιαστή (κύμα δίπλα στο wordmark αντί από πάνω), ειδικά για μπάρα.

  Νωρίτερα είχα κόψει στο wordmark για να μεγαλώσει — αλλά αυτό **πετούσε το κύμα**
  και η μπάρα έδειχνε κομμάτι του λογότυπου αντί για το λογότυπο. Δεν ξαναγίνεται:
  υπάρχει ένα μόνο αρχείο λογότυπου.
- **Favicon set** (svg/ico/apple-touch/192/512) — viewBox `103 198 62 62`, η τομή
  καθέτου–κύματος χωρίς το κείμενο (που στα 32px γίνεται λάσπη). Λευκό πάνω στο
  σκούρο του brand και **όχι** διαφανές: διαφανές λευκό εξαφανίζεται σε ανοιχτό
  browser chrome. Όλα τα μεγέθη βγαίνουν από το ίδιο vector, ώστε να μη διαφέρει
  το εικονίδιο ανάλογα με το ποιο format διαλέγει ο browser.

**Μασάζ ΚΑΤ' ΟΙΚΟΝ** — άλλαξε τη θέση όλου του site. Δεν υπάρχει κατάστημα, άρα:

- **Καμία διεύθυνση δεν δημοσιεύεται.** Στη θέση της μπαίνει η περιοχή κάλυψης
  (`site.serviceArea`) σε footer και ενότητα ραντεβού. Το LocalBusiness schema
  παραλείπει `streetAddress`/`postalCode` (κενά πεδία θα ήταν χειρότερα από την
  απουσία τους) και εκπέμπει `areaServed`.
- **Το SEO γύρισε σε «μασάζ κατ' οίκον Αθήνα»** — υψηλής πρόθεσης τοπικό keyword.
- **Η ενότητα «Ο χώρος» καταργήθηκε** και έγινε «Πώς γίνεται στο σπίτι»: τι χρειάζεται
  ο πελάτης να έχει, τι φέρνουμε εμείς.
- ⚠ Το `serviceArea` («Αθήνα και γύρω περιοχές») είναι **υπόθεση** — θέλει επιβεβαίωση.

**Μία φωτογραφία, μόνο στο hero.** Η ίδια εικόνα σε τρεις ενότητες διαβαζόταν σαν
γέμισμα — και χειρότερα, φωτογραφίες δωματίου περιέγραφαν χώρο που δεν υπάρχει.
Οι δύο εσωτερικές φωτό αφαιρέθηκαν (τα πρωτότυπα μένουν στο `raw/`).

**Γραμμικά σύμβολα** — τα σχήματα στο `src/lib/icons.ts`, ο renderer στο
`BrandIcon.astro`. Σχεδιασμένα στη **γλώσσα του λογότυπου**: ίδιο πάχος γραμμής,
ανοιχτές καμπύλες, μηδέν γεμίσματα, μηδέν πλαίσια. Επίτηδες **όχι** έτοιμο icon set —
λωτοί, πέτρες και χούφτες θα υπονόμευαν brand που όλη του η ταυτότητα είναι δύο λεπτές
γραμμές. Το σπίτι έχει για δάπεδο **το ίδιο το κύμα** του σήματος.
(Αν χρειαστούν άλλα: Lucide, Phosphor — άδεια MIT.)

| ομάδα | σύμβολα |
|---|---|
| πώς λειτουργεί | `home` · `table` · `one` |
| θεραπείες | `flow` (σουηδικό) · `depth` (deep tissue) · `focus` (θεραπευτικό) · `sports` (sports massage) · `cups` (cupping) · `pulse` (Theragun) |
| αλλού | `time` · `calendar` |

Ένα σύμβολο ανά θεραπεία — **έξι**. Το `face` έμεινε αχρησιμοποίητο όταν έφυγε η
θεραπεία προσώπου· δεν διαγράφηκε, σε περίπτωση που επιστρέψει.

⚠ **Γιατί τα σχήματα είναι σε `.ts` και όχι μέσα στο component:** τα content αρχεία
χρειάζονται τον τύπο `IconName`, και **δεν γίνεται import τύπου από αρχείο `.astro`** —
ο bundler προσπαθεί να διαβάσει το markup σαν TypeScript και το build σκάει με
παραπλανητικό μήνυμα («Unexpected "|"» σε γραμμή που δεν έχει καν `|`).

**`TreatmentList.astro`** — οι θεραπείες ως ήσυχο accordion. **Μηδέν JavaScript**:
native `<details>`/`<summary>` κάνει όλη τη δουλειά, οπότε πληκτρολόγιο, screen readers
και η **εύρεση στη σελίδα** του browser (που ανοίγει κλειστή ενότητα για να δείξει
το αποτέλεσμα) δουλεύουν από μόνα τους. Χειροποίητο accordion χαλάει τουλάχιστον ένα
από αυτά. Το «ένα ανοιχτό τη φορά» βγαίνει από το `name` attribute — accordion του ίδιου
του προτύπου. Η κίνηση ανοίγματος είναι `::details-content` + `interpolate-size`· όπου
δεν υποστηρίζεται (Firefox σήμερα) ανοίγει ακαριαία, που είναι το σωστό fallback.

**Αχρησιμοποίητο:** το `sections/FullBleedBreak.astro` έμεινε στο project αλλά δεν
καλείται πουθενά — επιστρέφει μόλις υπάρχουν κι άλλες πραγματικές φωτογραφίες.

**Για /harvest:** το `ServicesNumbered` διαγράφηκε από το project (αντικαταστάθηκε από
το accordion), αλλά του είχα προσθέσει προαιρετικό `icon?` που **αντικαθιστά** τον
αριθμό. Αξίζει να περάσει στο `modules/sections/ServicesNumbered.astro` της βιβλιοθήκης.

**Φωτογραφίες — πραγματικές, εγκατεστημένες.** Χρησιμοποιούνται **ως έχουν**:
καμία επεξεργασία χρώματος, κανένα duotone, καμία σκίαση. Τα placeholders ήθελαν
επεξεργασία επειδή ήταν τυχαίο stock· αυτές είναι ήδη σωστά σκηνοθετημένες και
οτιδήποτε πάνω τους θα τις χαλούσε. Μόνο crop και WebP.
Οριζόντια 2400×1600 (62KB), κάθετη 1600×2400 (52KB) — άνετα κάτω από το όριο των
300KB. Τα πρωτότυπα μένουν τοπικά στο `raw/` και **δεν** ανεβαίνουν στο git.

## ✅ Το «top έργο» — τι μπήκε

- **Μεταβάσεις σελίδας** (Astro View Transitions). Δεν υπάρχει λευκό φλας ούτε
  ανανέωση — η σελίδα σβήνει και ξεδιπλώνεται η επόμενη, όπως ο Koulis με Barba.
  Επιβεβαιωμένο με μετρήσιμο τεστ: μια μεταβλητή στη μνήμη επιβίωσε της πλοήγησης.
  Τα scripts (reveal engine, μενού, φόρμα) ξαναδένονται σε `astro:after-swap` —
  αλλιώς η δεύτερη σελίδα θα φόρτωνε με το περιεχόμενο μόνιμα αόρατο.
- **Το κύμα ως υπογραφή** (`WaveRule.astro`). Η γραμμή του λογότυπου αντικαθιστά
  τις ουδέτερες γραμμές ανάμεσα στα sections και **ανοίγει από αριστερά** καθώς
  μπαίνει στο κάδρο, σαν να τη σχεδιάζει χέρι.
- **Editorial ρυθμός**: full-bleed φωτογραφία που βγαίνει από το container
  (`FullBleedBreak.astro`), με αποκάλυψη σε **σκίσιμο** και όχι fade.
- **Ραντεβού ως διαδικασία** (`BookingDialog.astro`): native `<dialog>` με 4 πεδία.
  Το `tel:` μένει ως fallback χωρίς JavaScript.
- **«Τι γίνεται στην ώρα σας»** — τρία βήματα, σε 3 στήλες ώστε να μη διαβάζεται
  σαν δεύτερο αντίγραφο της λίστας θεραπειών.
- **Γλώσσα αποκλειστικότητας**: «δεχόμαστε αποκλειστικά κατόπιν ραντεβού»,
  «ένας πελάτης τη φορά», «δεν πουλάμε σειρές συνεδριών εκ των προτέρων».

## 🐞 Bug του factory — ΔΙΟΡΘΩΘΗΚΕ (starter v1.6.0 → v1.6.1)

**Δύο** reveal variants του starter δεν αποκαλύπτονταν **ποτέ**: `reveal-clip`
και `reveal-mask-up`. Ο λόγος είναι κυκλικός — ο `IntersectionObserver` μετράει
το στοιχείο **αφού** εφαρμοστεί το `clip-path`. Ένα στοιχείο που κρύβει τον
εαυτό του με clip αναφέρει μηδενική επιφάνεια → δεν περνά ποτέ το threshold →
δεν παίρνει ποτέ `.in` → μένει αόρατο για πάντα.

Αποδείχθηκε σε απομονωμένο τεστ με το **ακριβές** CSS και engine του starter:

| variant | μηχανισμός | αποτέλεσμα |
|---|---|---|
| `reveal-up` / `zoom` / `blur` / `tilt` | transform, filter | ✅ paint-time, η γεωμετρία μένει ανέπαφη |
| `reveal-clip` | `clip-path` | ❌ ποτέ |
| `reveal-mask-up` | `clip-path` | ❌ ποτέ |

**Λύση:** `mask-size` αντί για `clip-path`. Είναι κι αυτό paint-time (άρα ο
observer βλέπει κανονική γεωμετρία) και — κρίσιμο — **δεν χρειάζεται wrapper**,
οπότε δουλεύει και σε σκέτο `<h2>` χωρίς παιδιά. Η πρώτη μου λύση (clip στα
παιδιά) θα είχε αφήσει το `reveal-mask-up` σπασμένο, αφού μπαίνει σε τίτλους.

Εφαρμόστηκε σε `starter/src/styles/tokens.css`, `VERSION` → **1.6.1**, τρέξαμε
`setup.sh` (επιβεβαιωμένο staged). Το site χρησιμοποιεί την ίδια λύση — μηδέν
τοπικά παρακαμπτήρια, ώστε το `/harvest` να μη μεταφέρει απόκλιση πίσω.

**Κράτηση online (Cal)** — `site.bookingUrl` είναι **κενό**. Μόλις μπει το link,
**κάθε** CTA του site (header, αρχική, About) γυρίζει αυτόματα σε πραγματική κράτηση
με νέα καρτέλα· όσο είναι κενό, ανοίγει ο διάλογος «αφήστε τα στοιχεία σας».
Ένα σημείο απόφασης: `src/lib/booking.ts` — τα τρία CTA δεν μπορούν να διαφωνήσουν.

**`PinnedSteps.astro`** — η ενότητα «Πώς λειτουργεί» **καρφώνει** και εναλλάσσει
περιεχόμενο αντί να τα δείχνει και τα τρία μαζί. Μία οθόνη scroll ανά βήμα.
Το scroll **δεν** παρακάμπτεται: η σελίδα κυλάει κανονικά, απλώς το panel μένει
κολλημένο (`position: sticky`) — καμία «αρπαγή» του τροχού, το scrollbar δεν λέει ψέματα.
Χωρίς JS ή με reduced-motion εμφανίζεται ως απλή στοιβαγμένη λίστα.

## 🟡 Χρειάζονται απόφαση

- ~~Πλήρης λίστα υπηρεσιών~~ — **έκλεισε**. Έξι υπηρεσίες, όπως δόθηκαν:
  Αθλητικό 60′/80′ · Σουηδικό 60′/80′ · Θεραπεία προσώπου · Θεραπεία με βεντούζες.
  **Χωρίς τιμές και χωρίς στήλη διάρκειας** (απόφαση πελάτη — οι τιμές δόθηκαν για
  ακρίβεια, όχι για δημοσίευση). Οι διάρκειες μένουν **μέσα στα ονόματα** γιατί έτσι
  δόθηκαν και γιατί χωρίς αυτές δύο ζεύγη γραμμών θα ήταν πανομοιότυπα.
- **Η χειροπρακτική αφαιρέθηκε από παντού** — δεν είναι στη λίστα του πελάτη. Ήταν σε
  h1, tagline, description, keywords και μία υπηρεσία. Επιβεβαιωμένο: `grep -ri
  "χειροπρακτ" src/ dist/` → μηδέν. Το h1 έγινε «Αθλητικό & σουηδικό μασάζ».
  Αν τελικά την παρέχει, ξαναμπαίνει — αλλά χρειάζεται και δική της περιγραφή.
- **Βιογραφικό / εκπαίδευση θεραπευτή** — η σελίδα «Ποιοι Είμαστε» δεν ισχυρίζεται **κανένα** προσόν, πτυχίο ή χρόνια εμπειρίας, γιατί δεν μου δόθηκαν. Σε σελίδα υγείας δεν εφευρίσκουμε τέτοια.
- **Social media** — `site.ts` → `social`, όλα κενά (δεν εμφανίζονται στο UI όσο είναι κενά).
- **Umami website ID** — `.env`, για analytics.
- **Τρόπος ραντεβού** — τώρα είναι **τηλεφωνικά** (το CTA πάει σε `tel:`). Αν θέλουμε φόρμα, χρειάζεται εγγραφή domain στο forms.polarzee.com.

## 🟢 Επόμενα βήματα (Φάση 2)

- Σελίδες: Υπηρεσίες, Επικοινωνία
- `/seo` → schema.org, local SEO · `/geo` → llms.txt, AI visibility
- `/qa` → το launch gate (4 viewports, score /13)
- `/legal` → ο συνδυασμένος νομικός σελιδοδείκτης (υπάρχει ήδη, θέλει config)
- Favicon set από το πραγματικό λογότυπο (τώρα το monogram του starter)

## Σημειώσεις σχεδιασμού (γιατί έγινε έτσι)

- **Ανοιχτή παλέτα** — απόφαση πελάτη, με βάση το nikoskoulis.com. Το φόντο τραβήχτηκε
  από τις δικές του CSS μεταβλητές: `--light #f6f3ee` · `--gray #edebe8`.
- **Το μελάνι μαλάκωσε** από το `#1c1d1d` της αναφοράς σε **`#33302c`** (ζεστό
  κάρβουνο) — το καθαρό μαύρο «κόβει» πάνω σε κρεμ. Μετρημένη αντίθεση **11.9:1**
  (AAA θέλει 7:1), οπότε δεν θυσιάστηκε τίποτα.
- **Το accent σκούρυνε** από `#97846c` σε **`#75644e`**. Το παλιό μετρούσε **3.25:1**
  και **έκοβε το AA** — και δεν είναι διακοσμητικό: το φοράνε τα eyebrow labels
  («Η ΠΡΟΣΕΓΓΙΣΗ», «ΡΑΝΤΕΒΟΥ») και το CTA link του μενού. Το νέο κάνει 5.1:1 στο
  φόντο και 4.8:1 στην alt ζώνη.
- `--color-muted #6e6a64` = 4.85:1 — το πιο ανοιχτό που περνάει AA. Μην το ανεβάσεις.
- **Το λογότυπο δεν χρειάστηκε δεύτερη έκδοση**: το `#FFFFFF` του export δέθηκε σε
  `currentColor`, οπότε το ίδιο αρχείο βγαίνει λευκό πάνω στη φωτογραφία και σκούρο
  μελάνι στην μπάρα. Γι' αυτό μπαίνει **inline** (`?raw`) και όχι με `<img src>` —
  ένα `<img>` είναι απομονωμένο έγγραφο και αγνοεί το `currentColor`.
- **Header στις άκρες της οθόνης**, όχι μέσα στο container: το λογότυπο και το μενού
  έχουν μόνο ένα edge gutter (16px κινητό → 24px desktop, όπως η αναφορά).
- **Τυπογραφία μενού**: η αναφορά τρέχει στα 13px. Εμείς κρατάμε το όριο των 17px —
  δεν διαπραγματεύεται σε site από το οποίο κλείνουν ραντεβού.
- **Γραμματοσειρές**: Manrope + Commissioner. Επιλέχθηκαν επειδή έχουν **ελληνικό subset** — Montserrat / Poppins / Jost / Raleway / Cormorant **δεν έχουν** και θα έσπαγαν σε fallback.
- **Χωρίς τιμές πουθενά** — ρητή απόφαση πελάτη.
- **Hero**: μηδέν JavaScript. Το crossfade είναι CSS keyframes με αρνητικά `animation-delay`, οπότε δεν υπάρχει μαύρο flash στο πρώτο paint.
- **Το λογότυπο ΔΕΝ επαναλαμβάνεται στο hero** — κάθεται ήδη στην μπάρα λίγα εκατοστά
  πιο πάνω. Στη θέση του μπαίνει ήσυχο κείμενο χαμηλά στο κάδρο (eyebrow → h1 →
  γραμμή → τοποθεσία), όπως η αναφορά που κρατάει το hero σχεδόν χωρίς λέξεις.
- **Το h1 είναι στο hero** και είναι μικρό επίτηδες (24→33px, light, uppercase, φαρδύ
  tracking). Σε premium site η κλίμακα βγαίνει από τη φωτογραφία και το κενό, όχι από
  μεγάλα γράμματα. Το section από κάτω γύρισε σε `h2` — ένα h1 ανά σελίδα (ελεγμένο).
- **`BrandVeil.astro` — ΜΟΝΟ το intro.** Κάλυπτε και τις μεταβάσεις, κρατώντας την
  πλοήγηση μέχρι να ολοκληρωθεί η χάραξη· **αυτό ήταν η αισθητή καθυστέρηση**.
  Μετρημένο μετά την αφαίρεση: **238ms** και **106ms** ανά αλλαγή σελίδας.
- **Η χάραξη μετακόμισε στο ίδιο το λογότυπο του header** (`BrandMark` prop `draw`).
  Ξαναπαίζει σε **κάθε** σελίδα, στη θέση του, πάνω από σελίδα που έχει ήδη φορτώσει —
  μηδέν JavaScript και μηδέν listener: με τα view transitions ο header ξαναγράφεται σε
  κάθε πλοήγηση, οπότε η CSS animation ξεκινά από μόνη της.
- Σειρά και στα δύο: το κύμα ανοίγει αριστερά→δεξιά → η κάθετος χαράζεται από κάτω →
  το wordmark ανεβαίνει σε εστίαση.

  **Γιατί δύο διαφορετικές τεχνικές:** το κύμα είναι *γεμισμένο σχήμα* — δεν έχει
  stroke, άρα το `stroke-dashoffset` δεν εφαρμόζεται· χαράζεται με `mask-size`. Η
  κάθετος είναι *πραγματικό `<line>` με stroke*, άρα εκεί γίνεται αληθινό
  `stroke-dashoffset`. Το μήκος της (216.1 μονάδες) υπολογίζεται από τις δικές της
  συντεταγμένες στο `make-logo-parts.py` και περνά ως `--slash-len` — δεν είναι
  γραμμένο στο χέρι πουθενά.

  Στην πλοήγηση το swap **περιμένει** τη χάραξη (τυλίγεται το `event.loader` του Astro),
  οπότε η επόμενη σελίδα κατεβαίνει πίσω από το βέλο. Intro ~2.4s, μετάβαση ~0.95s.
  **Μία φορά ανά session** το intro — νέα καρτέλα ή `sessionStorage.removeItem('brt_intro_seen')`.
  Πρέπει να μένει ο ΠΡΩΤΟΣ κόμβος μέσα στο `<body>`: κλειδώνει πριν το paint.

  ⚠ **Παγίδα που κόστισε ένα bug:** στο `BrandVeil` το style block είναι `is:global`,
  και μέσα σε τέτοιο το `:global()` **δεν είναι έγκυρο** — ακυρώνει σιωπηλά ολόκληρο
  τον selector. Η κάθετος δεν κινούνταν καθόλου γι' αυτόν τον λόγο. (Στο `BrandMark`
  το block είναι scoped, οπότε εκεί το `:global()` **χρειάζεται** — αντίθετος κανόνας.)

  ⚠ **Δεύτερο bug, από το μεγαλύτερο λογότυπο:** η μπάρα ψήλωσε και **κάλυπτε τον
  πρώτο σύνδεσμο του μενού** — το κλικ έπεφτε πάνω στο λογότυπο. Το ύψος του λογότυπου
  είναι πλέον **μία μεταβλητή** (`--nav-logo`) από την οποία παράγονται και το μέγεθος
  του σήματος και το κενό που κρατάει το overlay· δεν μπορούν να ξεσυγχρονιστούν ξανά.
- **Χρονισμός εισόδου** (μηδέν JavaScript — καθαρά `animation-delay`):

  | χρόνος | τι συμβαίνει |
  |---|---|
  | 0 – 2.2s | το βέλο, λογότυπο σε εστίαση |
  | 2.2 – 3.3s | το βέλο σβήνει |
  | 3.3 – 3.5s | **μόνο η φωτογραφία** — καθαρή παύση, τίποτα δεν διασταυρώνεται |
  | 3.5 – 5.4s | το κείμενο ξεδιπλώνεται κλιμακωτά (eyebrow → h1 → γραμμή → τοποθεσία → βελάκι), 1.9s το καθένα με 0.24s διαφορά |

  Χωρίς intro (ίδιο session ή JS κλειστό) το κείμενο μπαίνει στα 0.6s.

  **Λεπτομέρεια που κόστισε ένα bug:** ο χρονισμός κλειδώνει στην κλάση `intro-ran`,
  **όχι** στην `preloader-active`. Η δεύτερη αφαιρείται όταν φεύγει το βέλο, και ένα
  delay που εξαφανίζεται στη μέση κάνει τον browser να ξαναϋπολογίσει τον χρόνο που
  πέρασε — το κείμενο εμφανιζόταν ακαριαία, έτοιμο, αντί να ξεδιπλωθεί.
- **Art direction κατά προσανατολισμό** (όχι πλάτος): κάθε portrait οθόνη — κινητό ή tablet όρθιο — παίρνει την κάθετη φωτό.
