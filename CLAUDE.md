# Body Reset Treatment — project notes

Μασάζ **κατ' οίκον** στην Αθήνα. Δεν υπάρχει κατάστημα: η θεραπεία πηγαίνει στον
πελάτη. Αυτό καθορίζει σχεδόν κάθε απόφαση παρακάτω.

- **Starter:** v1.6.1 (Astro rail) · **Tier:** Premium · **Host:** Cloudflare Pages
- **Εκκρεμότητες & αποφάσεις:** `TODO.md` — διάβασέ το πριν αγγίξεις περιεχόμενο
- **Πλάνο τελευταίας φάσης:** `~/.claude/plans/bubbly-dreaming-thacker.md`

## Standing rules (πέρα από το contract του starter)

| Κανόνας | Γιατί |
|---|---|
| **Καμία τιμή, καμία στήλη διάρκειας** | Ρητή απόφαση πελάτη. Οι τιμές δόθηκαν για ακρίβεια, όχι για δημοσίευση. |
| **Καμία χειροπρακτική** | Δεν παρέχεται. Σε σελίδα υγείας δεν διαφημίζουμε υπηρεσία που δεν γίνεται. |
| **Καμία διεύθυνση** | Υπηρεσία κατ' οίκον. Παντού μπαίνει `site.serviceArea`, ποτέ `address.street`. |
| **Μία φωτογραφία, μόνο στο hero** | Δεν υπάρχει χώρος να δείξουμε· η επανάληψη διαβαζόταν σαν γέμισμα. |
| **Μηδέν ισχυρισμοί προσόντων** | Δεν δόθηκαν πτυχία/χρόνια εμπειρίας. Δεν εφευρίσκονται. |
| **Τυπογραφικό όριο 17px** | Η αναφορά (nikoskoulis.com) τρέχει στα 13px· εμείς όχι. |

## Design tokens (`src/styles/tokens.css`)

Παλέτα από τις CSS μεταβλητές του nikoskoulis.com: `--light #f6f3ee` (φόντο),
`--gray #edebe8` (alt ζώνες). Το μελάνι μαλάκωσε σε **`#33302c`** (11.9:1) και το
accent σκούρυνε σε **`#75644e`** επειδή το προηγούμενο έκοβε το AA (3.25:1).

Γραμματοσειρές: **Manrope** (τίτλοι) + **Commissioner** (σώμα) — επιλέχθηκαν επειδή
έχουν **ελληνικό subset**. Montserrat / Poppins / Jost / Raleway / Cormorant **δεν
έχουν** και θα έπεφταν σε fallback.

## Τι είναι ιδιαίτερο εδώ

| Component | Τι κάνει |
|---|---|
| `BrandMark.astro` | Το λογότυπο, `draw` prop → ξανασχηματίζεται σε κάθε σελίδα στο header |
| `BrandVeil.astro` | Το intro (μία φορά ανά session) — **μόνο σε σελίδες χωρίς hero**. Στην Αρχική στέκεται στην άκρη (`skip`) και απλώς μαρκάρει τη συνεδρία. **ΔΕΝ** καλύπτει πλοήγηση |
| `WaveRule.astro` | Το κύμα του λογότυπου ως διαχωριστικό, ανοίγει στο scroll |
| `BrandIcon.astro` + `lib/icons.ts` | Εννέα γραμμικά σύμβολα, χαράζονται στο scroll |
| `PinnedSteps.astro` | ΕΝΑ component, δύο χρήσεις: απλά βήματα ή **βιτρίνα** (κατηγορία + τίτλος + μία γραμμή + φωτογραφία) όταν δοθεί `image`. Η φωτό ζει ΜΕΣΑ στο βήμα, ώστε να αλλάζει μαζί του και να δουλεύει και χωρίς JS |
| `HeroFullBleed.astro` | Full-screen, art direction κατά **προσανατολισμό** όχι πλάτος. **Κάνει το ίδιο το intro**: κρεμ οθόνη → το λογότυπο χαράζεται στη θέση του → η φωτογραφία εμφανίζεται από πίσω και μετά **αναπνέει** (zoom 20s μέσα/έξω, μόνο transform). Χωρίς ορατό κείμενο· ο h1 μένει ως `.sr-only`. `ink` prop → η φωτογραφία ορίζει μελάνη/scrim/χρώμα μπάρας |
| `scripts/make-logo-parts.py` | Ξαναφτιάχνει το `logo-parts.svg` από το export του Illustrator — 4 **αδελφά** groups |
| `scripts/probe-showcase.mjs` | Μετράει τη βιτρίνα: 6 θεραπείες × 8 μεγέθη — κοπή, σειρά βαφής, αντίθεση κάθε κειμένου πάνω στη φωτογραφία |
| `scripts/probe-hero-mark.mjs` | Δειγματοληπτεί τον βρόχο του hero σε ακριβείς χρόνους (pause + `currentTime`) |
| `NavOverlay.astro` | Διάφανη μπάρα, hamburger σε όλα τα breakpoints· κρύβει το λογότυπό της πάνω από hero που δείχνει ήδη το σήμα |
| `LangSwitch.astro` | ΕΛ / EN σε κεφαλίδα + υποσέλιδο. **Σήμανση, όχι λειτουργία** — το ανενεργό είναι `<span>`, όχι `href="#"` |
| `StepsRail.astro` | Τα ίδια βήματα οριζόντια: 3 μαζί ≥64rem, ράγα με swipe + κουκκίδες πιο κάτω. CSS scroll-snap, όχι βιβλιοθήκη |
| `PhoneFab.astro` | Sticky κουμπί κλήσης, μόνο τηλέφωνο. Σε desktop το `tel:` δεν κάνει τίποτα → αντιγράφει τον αριθμό |
| `pages/ypiresies/[slug].astro` | Μία σελίδα ανά θεραπεία, από το services.ts. `Service` JSON-LD + γειτονικοί σύνδεσμοι |
| `BookingDialog.astro` | Native `<dialog>`, 4 πεδία, `[data-booking]` το ανοίγει (listener σε **capture**) |
| `Footer.astro` | Κουβαλάει το μπλοκ ραντεβού + `id="epikoinonia"` → υπάρχει σε κάθε σελίδα |

## Παγίδες που κόστισαν bugs

1. **Ποτέ `clip-path` σε στοιχείο που παρακολουθεί ο IntersectionObserver.** Μετράει
   την επιφάνεια μετά το clip → μηδέν → δεν αποκαλύπτεται ποτέ. Χρησιμοποίησε
   `mask-size` (διορθώθηκε και στο starter v1.6.1).
2. **`:global()` σε `is:global` style block είναι ΑΚΥΡΟ** και ακυρώνει σιωπηλά όλο τον
   selector. Σε *scoped* block χρειάζεται. Αντίθετοι κανόνες.
3. **Δεν γίνεται `import type` από `.astro`** — ο bundler διαβάζει το markup σαν TS.
   Γι' αυτό τα σύμβολα είναι σε `lib/icons.ts`.
4. **Το ύψος του λογότυπου είναι μία μεταβλητή** (`--nav-logo`). Όταν ήταν δύο, η
   μπάρα κάλυπτε τον πρώτο σύνδεσμο του μενού και έτρωγε το κλικ.
5. **`<g>…</g>` δεν πιάνεται με non-greedy `.*?`** — τα groups του Illustrator είναι
   τριών επιπέδων, οπότε το πρώτο `</g>` κλείνει *εσωτερικό* group. Το παλιό script
   παρήγαγε SVG με ανοιχτά tags· ο browser το «διόρθωνε» βάζοντας `.lg-slash` και
   `.lg-word` **μέσα** στο `.lg-wave`, οπότε η μάσκα του κύματος έκοβε όλο το λογότυπο.
6. **Το `--nav-bar-h` ζει στο `tokens.css`, όχι στο NavOverlay.** Το hero μετράει από
   αυτό για να μην περάσει κάτω από την μπάρα· όσο ήταν κλεισμένο μέσα στο component,
   κάθε άλλος που το χρειαζόταν έγραφε δεύτερο νούμερο.
7. **Media query που δηλώνεται ΠΑΝΩ από τον βασικό κανόνα χάνει σιωπηλά** — ίδια
   ειδικότητα, αποφασίζει η σειρά. Το `--mark-w` του hero δεν άλλαζε σε κοντές οθόνες
   ακριβώς γι' αυτό.
8. **Ένα λογότυπο στην οθόνη τη φορά.** Η μπάρα κρύβει το δικό της όσο πατάει σε hero
   που δείχνει ήδη το σήμα. Το πρώτο paint το πιάνει `:has()` (πριν τρέξει JS), από κει
   και πέρα ο ίδιος observer με τη μελάνη. `visibility`, όχι μόνο `opacity` — link με
   opacity 0 παραμένει focusable και το tab πέφτει σε αόρατο στόχο.
9. **Το `intro-ran` επιβιώνει view transition.** Όσο το hero κλείδωνε την καθυστέρησή
   του πάνω του, η άφιξη στην Αρχική ΑΠΟ εσωτερική σελίδα κληρονομούσε 3.3s αναμονή
   χωρίς κανένα veil στην οθόνη. Τώρα δεν υπάρχει καθόλου σύζευξη.
10. **Ο router των view transitions ακούει κλικ ΠΡΩΤΟΣ.** Ο handler του dialog ήταν
    σε bubble, οπότε ο router είχε ήδη διαβάσει το event ως μη-ακυρωμένο και έφευγε
    σε άλλη σελίδα αντί να ανοίξει η φόρμα. Φαινόταν σωστό μόνο στην Αρχική, όπου το
    `/#epikoinonia` είναι άγκυρα της ίδιας σελίδας. Λύση: `addEventListener(..., true)`.
11. **Reveal + οριζόντια ράγα δεν συνυπάρχουν.** Κάρτα παρκαρισμένη δεξιά δεν μπαίνει
    ποτέ στο viewport → ο IntersectionObserver δεν τη σημειώνει → μένει στο opacity 0
    και ο χρήστης κάνει swipe σε κενό. Το reveal ανά κάρτα απενεργοποιείται στη ράγα.
12. **Το κουτί κόβει, όχι η φωτογραφία.** `object-fit: cover` σε κουτί με ΑΛΛΟΝ λόγο
    από την εικόνα πετάει τη διαφορά. Κουτί 4:3 με φωτό 2:3 στο κινητό έτρωγε τα δύο
    τρίτα του κάδρου. Ο λόγος του κουτιού = ο λόγος της πηγής (`--img-ratio`), και
    όπου δεν χωράει, ορίζεις **ύψος** και αφήνεις το πλάτος να ακολουθήσει.
13. **`align-content: center` ξεχειλίζει ΚΑΙ προς τα πάνω.** Περιεχόμενο ψηλότερο από
    το κουτί κεντράρεται βγαίνοντας εξίσου κι από τις δύο πλευρές — έτσι η ετικέτα
    της καρφωμένης ενότητας πήγαινε κάτω από τη σταθερή μπάρα. Λύση: `safe center`.
14. **`overflow: hidden` κρύβει και τα σφάλματά σου.** Τα βελάκια της καρφωμένης
    ενότητας σπρώχνονταν εκτός οθόνης στο κινητό, αλλά η σκηνή τα έκοβε — υπήρχαν
    στο DOM, δεν πατιόνταν, και κανένα page-level overflow test δεν τα έπιανε.
15. **`transform` κάνει το στοιχείο containing block.** Όσο η κίνηση εισόδου ήταν στο
    `.pinned-step`, η full-bleed φωτογραφία εγκλωβιζόταν στο κουτί του κειμένου
    (358×149 αντί για όλη την οθόνη). Μεταφέρθηκε στο `.pinned-step-copy`.
16. **Αρνητικό z-index δεν εγγυάται «από κάτω».** Η εικόνα έβαφε πάνω από την ετικέτα
    παρότι ήταν στο -1. Ανέβασε ρητά το ΠΕΡΙΕΧΟΜΕΝΟ, μην κατεβάζεις το φόντο.
17. **Σκίαση δεμένη στο ΚΕΙΜΕΝΟ, όχι στη φωτογραφία.** Το κείμενο έχει σταθερό ύψος σε
    px, η οθόνη όχι — σε κοντό τηλέφωνο η τρίτη γραμμή έπεφτε κάτω από το σβήσιμο (1.01:1).
18. **`pointer-events: none` κρύβει τη σκίαση από το `elementFromPoint`.** Ο έλεγχος
    έλεγε «το κείμενο είναι από πάνω» ενώ ξεπλενόταν. Μέτρα ΕΙΚΟΝΟΣΤΟΙΧΕΙΑ.
19. **Δηλώσεις στον ΙΔΙΟ κανόνα: η τελευταία κερδίζει.** Το `height: var(--zoom)` γράφτηκε
    πριν από ένα `height: auto` λίγες γραμμές πιο κάτω και δεν έκανε τίποτα. Δεν είναι
    θέμα ειδικότητας — είναι σειρά μέσα στο ίδιο μπλοκ.
20. **Πάνω σε φωτογραφία: φωτοστέφανο, όχι σκίαση.** Το wash έσβηνε το ένα τρίτο του
    κάδρου· `text-shadow` σε χρώμα σελίδας κοστίζει μόνο λίγα pixel γύρω από κάθε
    γράμμα και αφήνει τη φωτογραφία φωτογραφία.
21. **Μέτρα το ΤΟΠΙΚΟ περιβάλλον του γράμματος.** Κρύβοντας το κείμενο για να δεις το
    φόντο κρύβεις ΚΑΙ το φωτοστέφανο — ο έλεγχος έδινε 2.03:1 για κείμενο που μετριέται
    6:1 εκεί που πραγματικά κάθεται. Και βρες τη μελάνη με ΧΡΩΜΑ, όχι με φωτεινότητα:
    γκρι pixel φωτογραφίας περνιόταν για χρυσό τίτλο και μετριόταν με τον εαυτό του.
22. **Grid στήλη `auto` δεν συρρικνώνεται κάτω από το min-content.** Η καρφωμένη σκηνή
    στρωνόταν 387px σε οθόνη 360 και το `overflow: hidden` το έκρυβε τέλεια — καμία
    μπάρα κύλισης, μόνο μετατοπισμένα κουτιά. Λύση: `grid-template-columns: minmax(0,1fr)`.
23. **`color-mix(... , transparent)` δεν μετριέται.** Ημιδιαφανές γράμμα παίρνει το
    τελικό του χρώμα από ό,τι είναι από πίσω, οπότε πάνω σε φωτογραφία αλλάζει καρέ
    το καρέ. Πάνω σε εικόνα, ΑΔΙΑΦΑΝΗ χρώματα.
24. **`--fab-size`/`--fab-inset` στο tokens.css.** Το sticky κουμπί είναι fixed στη
    γωνία· ό,τι άλλο φτάνει εκεί κρατά χώρο ΑΠΟ ΑΥΤΑ, όχι από μαντεμένο νούμερο.
25. **`svh`, όχι `dvh`, σε ό,τι είναι sticky.** Το `dvh` αλλάζει καθώς κρύβεται η
    μπάρα διευθύνσεων του κινητού, οπότε η καρφωμένη ενότητα ξαναμετριόταν στη μέση
    της κύλισης — κόλλαγε και μετά πηδούσε. Το `svh` δεν κουνιέται.
26. **`scroll-snap-type: x mandatory` σε λωρίδα μέσα σε άρθρο = μάχη αξόνων.**
    Το `proximity` κουμπώνει όταν το εννοείς και κάνει άκρη όταν όχι. Ούτε
    `overscroll-behavior-x: contain` — εμποδίζει τη χειρονομία να φτάσει στη σελίδα.
27. **`max-height: 46rem` σε media query = ρύθμιση zoom μεταμφιεσμένη.** Το zoom δεν
    αλλάζει το παράθυρο· αλλάζει πόσα CSS pixel χωράνε μέσα του. Έτσι ΤΟ ΙΔΙΟ φυσικό
    παράθυρο (1536×714) έπεφτε πότε πάνω και πότε κάτω από το κατώφλι: στο 90% το σήμα
    του hero ήταν 461px, στο 100% **261px** — 43% διαφορά από ένα κλικ zoom. Ό,τι
    αποφασίζει μέγεθος ή κοπή θέλει `vw`/`svh`/**λόγο οθόνης**, ποτέ `rem`/`px`.
    Ο λόγος οθόνης είναι το ίδιο νούμερο σε κάθε zoom.
28. **Ένα γραφικό δεν είναι επικεφαλίδα.** Το `aria-label` σε inline SVG δεν διαβάζεται
   αξιόπιστα ως κείμενο h1 από τα crawlers — όταν φύγει το ορατό κείμενο του hero,
   πρέπει να μείνει πραγματικό κείμενο σε `.sr-only`.

## Εντολές

```bash
npm run dev        # ανάπτυξη
npm run build      # παραγωγή
npm run preview    # έλεγχος του build
QA_BASE="http://localhost:4321" QA_ROUTES="/,/poioi-eimaste,/oroi-xrisis-kai-politiki-aporritou" \
  node scripts/qa-screenshots.mjs     # launch gate, οπτικό μέρος
```

Τα build scripts για λογότυπο/σύμβολα/φωτογραφίες ζουν στο scratchpad της συνεδρίας —
τα πρωτότυπα αρχεία είναι στο `raw/`.
