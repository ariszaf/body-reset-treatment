---
/** Max-width wrapper with fluid inline padding. */
---

<div class="container">
  <slot />
</div>

<style>
  .container {
    width: 100%;
    max-width: var(--container-max);
    margin-inline: auto;
    padding-inline: var(--space-container);
  }
</style>
